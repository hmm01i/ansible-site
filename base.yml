---
- name: base playbook
  hosts: all
  gather_facts: true
  become: true
  tasks:
    - name: default packages
      package:
        name: "{{ item }}"
        state: present
      with_items:
        - zsh

    - import_tasks: tasks/users.yml

  # we don't necessarily whant this on laptops...
  #    - import_role:
  #      name: ansible-sshd
